# 第五节 数值方法：公式说明与演算示例

> **文件性质**：独立方法学附录（2026-09-14 撰）。
> **内容基准**：全部数值取自第五节数据分析工具的真实输出（`data/bili/bili_category_stats.csv`、`bili_order_tests.csv`、`lexicon_sensitivity.csv`、`weread_coding_summary.json`、`douban_2046909{,_merged}_stats/stats.json`、`douban_2046909_auth/keywords_top.csv`），
> 并以脚本 `scripts/verify_numeric_methods.py` 复用原始实现**逐值复算比对通过**（见第七节）。
> **数据时点**：2026-09-11/12；均为平台前端**可观测**材料，非概率抽样。

---

## 一、方法科学性核查（结论先行）

第五节声明采用的三项数值方法——**比例估计用 Wilson 95% 置信区间、组间差异用两比例 z 检验、分词用 jieba 与 TF-IDF**——经核查**科学有效，予以保留**。判定依据如下：

| 方法 | 科学性判定 | 理由 | 失效条件（须在文中设限） |
|---|---|---|---|
| Wilson 95% CI | **有效** | 得分区间；小样本、极端比例（近 0/1）下覆盖率远优于 Wald 正态近似，且不会越出 [0,1] | 仅对"观测语料"成立，不推断平台总体 |
| 两比例 z 检验（合并方差） | **有效** | 两组为**相互独立**样本（如"综合排序"与"最新排序"为不同检索结果的独立集合；两组样本量可不等）；教材级常规检验 | 不适用于同一单位的配对测量；不适用于小期望频数（此处各格计数均 >5，满足） |
| jieba + TF-IDF | **有效（探索性）** | 中文分词＋子线性 TF＋平滑 IDF 的常规加权，用于高频实词/主题结构描述；无黑箱模型 | 属**描述性**加权，不作显著性推断；结果随分词与停用词口径而变 |

三点补充限定，与正文数据纪律一致：
1. **无黑箱**：三项方法均由可读脚本实现（`bili_analyze.py` 的 `wilson_ci` / `two_prop_z`，`douban_analyze.py` 的 `tfidf_keywords`），参数在分析前固定。
2. **启发式指标不入结论**：标题耸动度、SnowNLP 情感分等启发式量仅存档，不进入正文与图版。
3. **非概率抽样**：Wilson 区间与 z 检验的条件推断**仅对观测语料成立**，不外推"平台总体占比""用户整体偏好""历时趋势"。

---

## 二、数据与口径总览（真实样本）

| 平台 | 语料 | 真实样本量 | 用于本附录的检验 |
|---|---|---|---|
| B站 | 视频标签（三关键词 × 综合/最新双轨） | 去重后 **N=797**；单格如 140 / 90 / 143 / 136 / 132 / 156 | Wilson CI、两比例 z |
| 豆瓣 | 《故事新编》（#2046909）"读过"短评 | WorkBuddy **497**（有评分 455）；豆包交叉核验 **200**（有评分 193） | 两比例 z（双智能体）、TF-IDF |
| 微信读书 | bookId 40509933 公开点评/划线（分层 S1/S3） | **S1=209、S3=827**（合计 1036） | Wilson CI、两比例 z（词表敏感性） |
| 抖音 | 可见性快照 | "鲁迅说"147（相关 136）、"故事新编 鲁迅"18 | 描述性占比（不作检验） |

---

## 三、比例估计：Wilson 95% 置信区间

### 3.1 公式

样本量 n、命中 x，点估计 p̂ = x/n，置信水平 95% 取 z = 1.96：

```
中心   p̃ = ( p̂ + z²/(2n) ) / ( 1 + z²/n )
半宽   h  = z / (1 + z²/n) · √[ p̂(1−p̂)/n + z²/(4n²) ]
区间   CI₉₅% = ( p̃ − h , p̃ + h )
```

**公式文字说明**：Wilson 区间由"得分检验"反解得到——它对中心做了向 0.5 的收缩（分子 p̂ + z²/2n、分母 1+z²/n），故当 p̂ 接近 0 或 1 时不会像 Wald 区间那样越界；半宽中的 z²/(4n²) 项正是该收缩带来的方差修正。

### 3.2 演算示例一（B站"文学品质类"，综合排序，「故事新编 鲁迅」）

真实输入：x = 112，n = 140（占比 112/140 = 80.00%），z = 1.96，z² = 3.8416。

```
z²/n     = 3.8416 / 140            = 0.0274400
z²/(2n)  = 3.8416 / 280            = 0.0137200
z²/(4n²) = 3.8416 / (4×19600)      = 0.0000490

p̃ = (0.8000 + 0.0137200) / (1 + 0.0274400)
   = 0.813720 / 1.027440            = 0.791987

项内和 = p̂(1−p̂)/n + z²/(4n²)
       = (0.8×0.2)/140 + 0.0000490
       = 0.001142857 + 0.0000490     = 0.001191857
√项   = 0.034523

h = (1.96 / 1.027440) × 0.034523 = 1.90766 × 0.034523 = 0.065858

CI₉₅% = (0.791987 − 0.065858 , 0.791987 + 0.065858)
      = (0.726129 , 0.857845)
```

**结果：80.00%，Wilson 95% CI = (72.61%, 85.78%)** —— 与存档 `bili_category_stats.csv`（72.61 / 85.78）**完全一致**。

### 3.3 演算示例二（微信读书"文学专业维度"，分层 S1 点评）

真实输入：x = 73，n = 209（占比 34.93%）。

```
z²/n = 3.8416/209 = 0.0183810 ；z²/(2n)=0.0091905 ；z²/(4n²)=0.00002199
p̃   = (0.349282 + 0.0091905)/(1 + 0.0183810) = 0.358472/1.018381 = 0.352002
项内和 = (0.349282×0.650718)/209 + 0.00002199 = 0.00108749 + 0.00002199 = 0.00110948
√项 = 0.033308 ；h = (1.96/1.018381)×0.033308 = 0.064106
CI₉₅% = (0.287896 , 0.416108)
```

**结果：34.93%，Wilson 95% CI = (28.79%, 41.61%)** —— 与 `weread_coding_summary.json` 的 `[0.2879, 0.4161]` 一致。

### 3.4 B站其余三项（存档值，同法算出）

| 关键词/排序/类别 | x/n | 占比% | Wilson 95% CI |
|---|---|---|---|
| 故事新编 鲁迅·综合·文学品质类 | 112/140 | 80.00 | [72.61, 85.78] |
| 故事新编 鲁迅·最新·文学品质类 | 87/90 | 96.67 | [90.65, 98.86] |
| 故事新编 鲁迅·综合·有声说书类 | 22/140 | 15.71 | [10.61, 22.65] |
| 故事新编 鲁迅·最新·有声说书类 | 63/90 | 70.00 | [59.87, 78.49] |

---

## 四、组间差异：两比例 z 检验

### 4.1 公式

两组**独立**样本（组1：x₁/n₁；组2：x₂/n₂），检验 H₀: p₁ = p₂：

```
合并比例   p̄ = (x₁ + x₂) / (n₁ + n₂)
标准误     SE₀ = √[ p̄(1−p̄) · (1/n₁ + 1/n₂) ]
检验统计量 z  = (p̂₁ − p̂₂) / SE₀     ，  双侧 p = 2(1 − Φ(|z|))
```

**公式文字说明**：H₀ 下两组共享同一比例，故用**合并**比例 p̄ 估计共同方差（`two_prop_z` 的 `p=(x1+x2)/(n1+n2)` 即此）；z 度量两组占比之差相当于其标准误的多少倍，再经标准正态换算为 p。

### 4.2 演算示例一（B站"有声说书类"，综合 vs 最新 —— 即文中 z = −8.32）

真实输入：综合 x₁=22 / n₁=140（15.71%），最新 x₂=63 / n₂=90（70.00%）。

```
p̂₁ = 22/140 = 0.1571429      p̂₂ = 63/90 = 0.7000000
p̄  = (22 + 63)/(140 + 90) = 85/230 = 0.3695652
SE₀ = √[ 0.3695652 × 0.6304348 × (1/140 + 1/90) ]
    = √[ 0.2329865 × 0.01825397 ]
    = √0.00425281 = 0.0652135
z   = (0.1571429 − 0.7000000) / 0.0652135 = −0.5428571 / 0.0652135 = −8.3243
双侧 p = 2(1 − Φ(8.3243)) ≈ 8.6×10⁻¹⁷ ≈ 0.000
```

**结果：z = −8.324，p ≈ 0（显著）** —— 与存档 `bili_order_tests.csv` 的 **−8.324** 一致。
（**重要厘清**：正文所报 z = −8.32 属于**有声说书类**；"文学品质类 80.0%→96.7%"的 z 是 −3.612，见下例。二者不可混记。）

### 4.3 演算示例二（B站"文学品质类"，综合 vs 最新）

真实输入：112/140（80.00%）vs 87/90（96.67%）。

```
p̄  = (112 + 87)/230 = 199/230 = 0.8652174
SE₀ = √[ 0.8652174 × 0.1347826 × 0.01825397 ] = √0.00212873 = 0.0461383
z   = (0.800000 − 0.966667)/0.0461383 = −0.166667/0.0461383 = −3.6123
双侧 p ≈ 0.0003
```

**结果：z = −3.612，p = 0.0003** —— 与存档一致。

### 4.4 演算示例三（微信读书：词表敏感性，S1 点评 vs S3 划线下想法）

检验"文学专业维度"占比在两层间是否稳定（**真实数据**，来自 `lexicon_sensitivity.csv`）：

```
完整词表：S1 73/209 (34.93%) vs S3 28/827 (3.39%)
  p̄  = (73 + 28)/(209 + 827) = 101/1036 = 0.0974903
  SE₀ = √[ 0.0974903 × 0.9025097 × (1/209 + 1/827) ]
      = √[ 0.0879861 × 0.00599388 ] = √0.000527357 = 0.0229643
  z  = (0.3492823 − 0.0338573)/0.0229643 = 0.3154250/0.0229643 = 13.736

严格版（仅文学批评术语）：S1 34/209 (16.27%) vs S3 9/827 (1.09%)
  z  = 9.83
```

**结果：完整词表 z = 13.74；严格版 z = 9.83（均显著）** —— 与存档一致；表明结论对词表删减具有稳健性。

### 4.5 演算示例四（豆瓣：双智能体"五星"占比，口径收敛）

真实输入：豆包 126/193（65.28%）；WorkBuddy 307/455（67.47%）。

```
p̄  = (126 + 307)/(193 + 455) = 433/648 = 0.6682099
SE₀ = √[ 0.6682099 × 0.3317901 × (1/193 + 1/455) ] = √0.00163611 = 0.0404489
z  = (0.6528497 − 0.6747253)/0.0404489 = −0.0218756/0.0404489 = −0.5408
双侧 p ≈ 0.589
```

**结果：z = −0.541，p ≈ 0.59（不显著）** —— 不拒绝"两智能体五星占比相同"，与正文"口径收敛、方向与量级一致"吻合。

### 4.6 一致性与澄清（避免误读）

- **B站比较为独立样本**：「综合排序」与「最新排序」是同一关键词下**两组不同的检索结果集合**（n 可不等，如 140 vs 90），故用 4.1 的独立两比例 z 检验；**不涉及**同一单位两次测量的配对结构，**不使用** McNemar。
- 正文所报 **z = −8.32** 对应 **有声说书类**（4.2）；文学品质类的检验值为 **−3.612**（4.3）。

---

## 五、分词与加权：jieba + TF-IDF

### 5.1 处理流程（`douban_analyze.py`）

1. **清洗**：正则 `[^\u4e00-\u9fa5A-Za-z0-9]+` 置为空格，去除标点。
2. **分词**：`jieba.cut(text)`（精确模式）。
3. **去停用词**：剔除内建中文停用词表（的、了、是、在……）与长度为 0 的词。
4. **加权**：以"每条短评为一篇文档"，对词项计算 TF-IDF，`min_df=2` 过滤，取 Top。

### 5.2 公式

```
TF(t,d)  = 1 + ln( c(t,d) )                 # 子线性（对数）词频，c 为 d 内计数
IDF(t)   = ln( N / (1 + df(t)) )            # 平滑文档频率，N 为文档数，df 为含 t 的文档数
TFIDF(t) = Σ_d [ TF(t,d) × IDF(t) ]         # 跨文档求和（本实现的排序量）
保留条件 : df(t) ≥ 2（min_df）
```

**公式文字说明**：子线性 TF（`1+ln c`）抑制高频词在单篇内的线性放大；平滑 IDF（`ln(N/(1+df))`）使**跨文档罕见词**获得更高权重；对每条短评求和得到全语料权重——这正是"鲁迅""铸剑"等专名权重高于"最""太"等虚词的原因。

### 5.3 演算示例（「铸剑」在 200 条豆瓣语料中的 TF-IDF）

真实参数：语料 N = 200 篇（`comments_2046909_P.jsonl`）；「铸剑」df = 34（出现在 34 篇），总词频 = 36。

```
IDF(铸剑) = ln( N/(1+df) ) = ln( 200/35 ) = ln(5.714286) = 1.74349

TFIDF(铸剑) = Σ_{d 含"铸剑"} (1 + ln c_d) × 1.74349
            = 1.74349 × Σ_d (1 + ln c_d)
            = 61.677     （Σ_d (1+ln c_d) ≈ 35.376）
```

**结果：TF-IDF("铸剑") = 61.677**，与存档 `keywords_top.csv`（铸剑：词频 36，权重 **61.677**）**完全一致**；同法复现「鲁迅」= 77.718（词频 67）、「故事」= 75.477（词频 57）、「喜欢」= 57.646（词频 31），均与存档一致。

---

## 六、边界与失效条件

1. **非概率抽样**：四平台语料均为"算法排序下可见"的观测集合，非全体用户/内容随机样本；上述区间与检验**仅对观测语料成立**。
2. **抖音为单源快照**：豆包单方采集、无对端交叉核验，结论止于"可见性结构描述"，不作总体或历时推断（其占比仅作描述性呈现，本附录不对其作 z 检验）。
3. **比例尺度**：占比比较以"同一平台语料内"为界；跨平台占比不可直接相减。
4. **TF-IDF 为描述性**：用于选词与主题结构刻画，不附带显著性；其结果依分词/停用词/`min_df` 口径而定。

---

## 七、复现与一致性核验

- **核验脚本**：`scripts/verify_numeric_methods.py`（复用 `bili_analyze.py` 的 `wilson_ci`/`two_prop_z` 与 `douban_analyze.py` 的 `tfidf_keywords` 原始实现）。
- **运行**：`python scripts/verify_numeric_methods.py`。
- **核验结论**（2026-09-14）：Wilson 四组区间、z 检验（−8.324 / −3.612 / +3.717 / +0.581）、微信读书词表敏感性（13.74 / 9.83）、豆瓣双智能体（−0.541）、TF-IDF（鲁迅 77.718、故事 75.477、铸剑 61.677、喜欢 57.646）**全部与存档输出逐值一致**。
