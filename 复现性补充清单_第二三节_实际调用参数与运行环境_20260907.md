# 复现性补充清单：论文未提及的实际调用工具、参数与运行环境（第二节·第三节）

> **编制目的**：本清单是《工具与模型清单报告_第二三节定稿_20260907.md》（论文文本层）的姊妹篇，专收**论文未明确提及、但真实数据分析过程中在 WorkBuddy 平台实际调用**的工具、参数与运行环境，据 `分析输出/scripts/` 归档脚本、数据产物与当前虚拟环境实测整理，供第三方完整复现。
> **标注规则**：【论文未提及】＝定稿正文未出现的信息；【论文已提及】＝定稿已载明（此处仅作对照锚定）；【实测】＝本次在运行环境中直接核验的值。
> **配套关系**：正文数值以 B方（人工校准口径，`故事新编_数字人文分析/`）为主；A方（机器重算口径，`分析输出/`）为独立复算。本清单对两方分别说明。

---

## 〇、两套实现的运行位置总览（论文未提及）

| 实现 | 运行位置 | 归档状态 | 说明 |
|---|---|---|---|
| **A方（机器重算版）** | WorkBuddy 本地虚拟环境（见§1） | **脚本已归档**：`分析输出/scripts/`（macro_l1_density.py、macro_l2_positions.py、macro_l3_semantic.py、micro_sentiment.py、micro_supplement.py、make_figures_final.py 等） | 脚本化全流程，输出 JSON 存 `分析输出/macro/`、`分析输出/micro/` |
| **B方（人工校准修订版，定稿主口径）** | 豆包工作侧执行 | **脚本未归档，仅数据产物归档**：`故事新编_数字人文分析/`（macro_l1/l2/l3、micro_sentiment、micro_network、micro_density 各 JSON） | 含人工标注、语境情感词典校准、场景人工切分；运行环境参数无法从本地还原 |
| **BERTopic 主题建模** | **BFSU BERTopic 平台（expert 模式，2026-09-04）**，非本地 venv | 输出存 `故事新编_结构化文本（豆包工作梳理版本）/BERTopic_Output`；分析文档：`分析输出/BERTopic跨篇主题比较与三角验证.md` | 本地 venv 【实测】**未安装** bertopic / hdbscan / spacy，证实该步骤非本地执行 |

---

## 一、运行环境（A方脚本实际执行环境）【论文未提及】

| 项目 | 实测值 | 备注 |
|---|---|---|
| 操作系统 | Microsoft Windows，版本 10.0.26200.9168（64位） | 【实测】 |
| Python | **3.13.14** | 【实测】WorkBuddy 托管虚拟环境：`C:\Users\HUAWEI\.workbuddy\binaries\python\envs\default\` |
| jieba | **0.42.1** | 分词 |
| snownlp | **0.12.3** | 【论文已提及】情感分析 |
| gensim | **4.4.0** | 词向量加载（依赖 smart_open 8.0.1） |
| scikit-learn | **1.9.0** | TF-IDF |
| networkx | **3.6.1** | 人物情感网络 |
| numpy | **2.5.2** | 数值计算 |
| matplotlib | **3.11.1** | 图A–G 绘制 |
| pandas | **3.0.5** / openpyxl **3.1.5** | 数据附录 xlsx 生成 |
| scipy | 1.18.1 | 依赖项 |
| 其余已装（本两节未直接用） | sentence-transformers 6.0.1、torch 2.14.0、transformers 5.16.1、umap-learn 0.5.12、pillow 12.3.0 | 为第四节 BERTScore 等后续工作所装；**bertopic、hdbscan、spacy 未安装** |

---

## 二、语料与输入配置【论文未提及】

| 参数名称 | 取值 | 说明 |
|---|---|---|
| A方语料 | `分析输出/corpus/{八篇}.txt`（豆包工作梳理版） | 段落＝换行切分（`split("\n")` 去空行）；《铸剑》162段／390句 |
| B方语料 | `故事新编_数字人文分析/corpus/`（视觉转写版） | 《铸剑》160段／483句；与A方切分规则不同，**绝对值不可直接对表** |
| BERTopic 输入 | 豆包梳理版八篇（文档声明与 `分析输出/corpus` MD5 逐字一致） | 分析单元＝约126 token 文档块，共 432 单元 |
| 文本变体归一化 | `"O.K"→"OK"`、`"O. K"→"OK"`、`"维他命 W"→"维他命"` | 分词前预处理，防止变体漏配（macro_l1_density.py L79） |
| 词元合法性正则 | `[一-鿿A-Za-z0-9]+` 全匹配 | 剔除标点，仅保留汉字/字母/数字词元 |

---

## 三、分词与自定义词典（第一、二层面；图A、图C）

**工具：jieba 0.42.1【论文未提及版本】**

| 参数名称 | 取值 | 选择依据/默认值说明 |
|---|---|---|
| 分词调用 | `jieba.lcut(text)` | **全部默认**：精确模式（cut_all=False）、HMM 新词发现开启（HMM=True）——脚本未传参，即为库默认 |
| 自定义词注入 | `jieba.add_word(w)` 逐词注入 | 不用 `load_userdict` 文件方式；将现代层＋历史层全部词条提升为切分优先词 |
| 词典来源 | B方底本 `故事新编_数字人文分析/macro_l1/lexicons.json` ＋ A方裁定修订 | 定稿词典：`分析输出/lexicons/lexicons_final.json` |
| 词典规模 | 现代层 **595词**、历史层 **408词**【实测，与定稿版数据附录文件行数一致】 | B方9月4日版为593词，A方裁定后定稿为595词 |
| 裁定新增 | **28词**（O.K、摩登、留声机、眼镜、巡警、巡士、警笛、制服、国语、文化、考察、试验、文学家、新闻、报告、诗人、诗歌、艺术、维他命、飞车、莎士比亚、古貌林、好杜有图、保险、资本、恋爱、图书馆、讲义、太太） | 每词附语料实证理由，全量记录于脚本（macro_l1_density.py L24–54）——替代论文所述"两名研究者独立标注协商"（脚本docstring明示"无法复现，改为规则化裁定并全量记录裁定理由"） |
| 裁定剔除 | **10词**（上帝、法律、跳舞、灵魂、自由、进步、文明、结婚、马力、世界） | 语境核查为古语境/普通词，附理由（L56–67） |
| 长词子串兜底 | L1密度：词长 **≥3字** 且未入分词结果则子串补计；L2位置：词长 **≥2字** | 防分词切碎漏检；两脚本阈值不同（影响计数，须注意） |
| 密度公式 | 现代层词次 ÷ 总词数（剔标点）×100% | 词元密度口径 |
| 句级口径核验 | `分析输出/macro/sent_level_check.json`（A方：铸剑390句/现代句15＝3.8%等八篇） | **生成脚本未单独归档**【论文未提及】 |
| 位置分布统计量 | 前/中/后三等分（`int(rel_pos*3)` 截断）、归一化熵（÷log 3）、相邻间隔变异系数 CV | macro_l2_positions.py；`rel_pos=段号/总段数` |

---

## 四、词向量与语义距离（第三层面；图B）

**工具：gensim 4.4.0 `KeyedVectors`【论文未提及版本】**

| 参数名称 | 取值 | 选择依据/默认值说明 |
|---|---|---|
| 模型文件 | `故事新编_数字人文分析/resources/light_Tencent_AILab_ChineseEmbedding.bin` | 腾讯AI Lab中文词向量**轻量版**【论文已提及】 |
| 词表/维度 | **143,613 词 × 200 维**【实测】 | 与论文"约14.4万词、200维"一致 |
| 加载参数 | `load_word2vec_format(path, binary=True, unicode_errors="ignore")` | binary=True 因 .bin 二进制格式；unicode_errors="ignore" 容错编码异常词【论文未提及】 |
| 余弦距离 | `1 − dot(a,b)/(‖a‖·‖b‖ + 1e-12)` | 含 1e-12 数值稳定项【论文未提及】 |
| M1口径 | 各篇语料实存词（分词命中或≥3字子串兜底）× 词表内词，**全配对**平均余弦距离 | 【论文已提及口径；实存词筛选规则论文未提及】 |
| 质心口径 | 两类词向量均值后再算余弦距离 | A方独立复算实现【论文已提及存在；实现细节论文未提及】 |
| 全局基准抽样 | `random.seed(42)`，现代/历史词典各抽 min(100, 词表内词数) 词，全配对 | **随机种子已固定＝42**，基准可复现【论文未提及】 |

---

## 五、情感分析（第三节；图D、图E）

**工具：SnowNLP 0.12.3【论文已提及版本；以下调用细节论文未提及】**

| 参数名称 | 取值 | 说明 |
|---|---|---|
| 调用方式 | `SnowNLP(s).sentiments`（无参，用库内置朴素贝叶斯模型） | 输出 0–1 |
| 区间映射 | `v = 2s − 1` → [−1, +1] | A方脚本实现 |
| 内置训练语料 | **pos.txt 16,548 条／neg.txt 18,576 条**【实测当前环境包内文件】 | 论文作"约1.6万/约1.8万"【已提及】；定稿版数据附录文件名原误作"neg_18575条"，**已于2026-09-07统一更正为18576条**（归档内容补回1条全角空白记录，与包内neg.txt字节一致） |
| 句子切分 | 正则 `[。！？…]+` 切分，去空白后非空白字符 **≥3** 才计为句 | A方390句、B方483句差异的来源之一 |
| 异常处理 | 单句评分异常时 `v=0.0` 兜底 | micro_sentiment.py L48–49 |
| 场景切分（A方，0基段号） | 月下捕鼠0–6／母亲述仇7–20／入城遇王21–54／交付头颅55–76／第一歌77–92／宫殿献技93–104／鼎中歌唱105–132／三头同煮133–137／辨头群像138–157／葬礼158–161 | 关键词锚定162段 |
| 场景切分（B方，1基段号） | 1–7／8–21／22–55／56–77／78–93／94–105／106–133／134–138／139–158／159–162 | 数据附录§5.1，与A方同构（仅编号基差） |
| 场景统计量 | mean_v、max_v、min_v、oscillation（相邻句情感差绝对值均值）、pos_ratio（正句占比） | 振荡度量【论文未提及】 |
| 人物指称正则 | 眉间尺：`眉间尺|孩子|少年|儿子的头|眉间尺的头`；宴之敖者：`宴之敖者|黑色人|黑衣人|黑的人`；楚王：`王|楚王|大王|王的头|王头` | 句内提及即计入（共现近似）【论文未提及具体模式】 |
| 歌谣行识别 | 行内含"兮"或"哈哈爱"，剥离"…—"后评分 | micro_supplement.py |
| **语境情感词典** | 数据源＝`故事新编_数字人文分析/micro_network/network_annotated.json` 之 `emotion_valence_map`，取值 [−1,+1] | **B方人工核定产物**【论文已提及存在；词典实体与生成过程为人工标注，不可脚本复现】 |
| 图D四处校准锚点 | (段65, +0.60) 信任确立；(段70, −0.20) 割头交付；(段119, +0.70) 鼎中歌唱；(段135, +0.30) 三头同煮 | 数据附录附录四/五；九点位全表见附录五5.1 |

---

## 六、语义密度计算（图F交叉验证）

**工具：自定义Python脚本＋scikit-learn 1.9.0 `TfidfVectorizer`【实现细节论文未提及】**

| 参数名称 | 取值 | 选择依据/默认值说明 |
|---|---|---|
| 停用词表 | 自建47词（"的了是在有和就不这那也都很要他她它们你我说着呢吧啊呀么之乎也于以其而为且与"＋"一个/没有/什么/自己/已经/但是/于是/因为/所以/然而/并且"） | 【论文未提及】 |
| 内容词筛选 | 正则 `[一-鿿]{1,4}` 且非停用词 | 1–4字汉字词 |
| TfidfVectorizer 显式参数 | `tokenizer=str.split`、`lowercase=False` | 预分词后按空格切；保留大小写 |
| TfidfVectorizer 默认参数 | `norm='l2'`、`use_idf=True`、`smooth_idf=True`、`sublinear_tf=False`、`min_df=1`、`max_df=1.0`、`dtype=float64` | **均为库默认（脚本未覆盖）**——sklearn 1.9.0 |
| TF-IDF 聚合 | `X.mean(axis=1)`：**对全词表（含零值项）取均值** | ⚠ A方实现的已知缺陷（定稿复算报告已声明：混入长度/稀疏度伪影、多场景舍入退化至同值0.0060）；B方口径为 tf×idf 段落级均值（脚本未归档，仅数据存 `故事新编_数字人文分析/micro_density/semantic_density_zhujian.json`） |
| TTR | `len(set)/len(tokens)`，词数 **≥4** 才计 | 短段剔除 |
| 词向量方差 | 场景/段内**去重词**在腾讯词向量中的向量，按维取方差后均值；向量数 **>2** 才计 | 与§四同一模型文件 |
| 复合得分 | TTR／TF-IDF／词向量方差各自 **z-score**（含1e-12稳定项）后**等权均值** | A方 z 复合；B方报告值为三维原始值 |

---

## 七、人物情感关系网络（图E）

**工具：NetworkX 3.6.1【论文未提及版本】**

| 参数名称 | 取值 | 说明 |
|---|---|---|
| 图类型 | `nx.Graph()`（**无向图**） | 三节点：眉间尺／宴之敖者／楚王 |
| 边权 | 场景内两人物**共现句数**累加（`weight`） | 共现＝同句同时命中两个指称正则 |
| 边情感 | 共现句 SnowNLP 情感均值（`mean_sent`） | A方原始评分口径（校准后以B方词典为准） |
| 中心性 | `G.degree(weight="weight")` 加权度中心性 | 楚王加权度=9，为最高（图E副题"缺席的在场"） |
| 最终图E绘制 | **matplotlib 手工布点绘制**（节点坐标硬编码、annotate 箭头），**非 nx.draw** | make_figures_final.py L122–140【论文未提及】 |

---

## 八、BERTopic 主题模型（图G）

**运行平台：BFSU BERTopic（expert 模式，2026-09-04）【论文未提及平台；本地未安装 bertopic，见§〇】**

| 参数名称 | 取值 | 选择依据/默认值说明 |
|---|---|---|
| 嵌入模型 | all-MiniLM-L12-v2 | 【论文已提及】英文句向量，语域失配已声明 |
| 中文分词 | **zh_core_web_sm**（spaCy 中文模型） | 【论文未提及】 |
| 分析单元 | 约 **126 token** 文档块，共 **432** 单元 | 切断段落边界（改进建议已记录） |
| min_topic_size | **10** | 【论文未提及】 |
| UMAP | `n_neighbors=2`（库默认15，论文已提及）、`n_components=4`【论文未提及】 | 低近邻数致簇碎裂 |
| 输出 | 17 有效主题（T0–T16）＋离群 T-1（160/432＝**37.0%**，各篇29.2%–44.4%） | 【论文已提及】 |
| 主题归属 | HDBSCAN **硬分配 membership**（Probability 列非校准概率） | 数据附录口径限制④ |
| 主题熵 | 非离群单元 Shannon 熵（图G(a) 数据源） | 【论文已提及】 |
| 未过滤高频功能词 | "说/里/两/想/走/吃"等进入主题词 | 【论文已提及】 |

---

## 九、图表绘制（图A–G）

**工具：matplotlib 3.11.1【论文未提及】**

| 参数名称 | 取值 | 说明 |
|---|---|---|
| 后端 | `matplotlib.use("Agg")`（无界面渲染） | 【论文未提及】 |
| 中文字体 | `font.sans-serif=["Microsoft YaHei", "SimHei"]`；`axes.unicode_minus=False` | 负号正常显示 |
| 输出分辨率 | 定稿版 **dpi=200**；2026-09-05放大重绘版 **dpi=300**（画布×~1.23、字号×~1.4，数据不变） | 双写至 `定稿版/figures/` 与 `定稿包03_图表/` |
| 数据来源 | 图A/B/C/F/G：脚本内**硬编码B方校准值**；图D：读 `micro/micro_zhujian_sentiment.json`（A方SnowNLP形态曲线）＋硬编码校准锚点星标 | 图D裸输出仅示形态（图注声明不作结论依据） |
| 图D平滑 | **5段滑动均值**（win=5，前后各取5段窗口平均） | 【论文未提及】 |
| 场景底色 | `plt.cm.Set3` 十色带，`alpha=0.4` axvspan | |
| 归档脚本 | make_figures_final.py（定稿七图）；make_figures_v2_CDE.py、make_figures_v3_D.py（过程版）；make_figF_large / make_figE_large / make_figures_large_ABCDG _20260905.py（放大版） | 数据附录§文件清单载明前三者 |

---

## 十、数据附录与表格生成【论文未提及】

| 工具 | 版本 | 用途 |
|---|---|---|
| openpyxl | 3.1.5 | `make_appendix_xlsx.py` 生成数据附录数值表.xlsx（11表） |
| pandas | 3.0.5 | 数据汇总与 CSV 输出 |
| make_pure_table.py | — | 纯数值表生成（脚本归档） |

---

## 十一、影响结果的配置细节与复现注意事项（汇总）

1. **随机性来源仅两处，均已固定**：语义距离全局基准抽样 `random.seed(42)`；BERTopic 由平台侧运行（UMAP 具随机性，平台种子不可考）。SnowNLP、jieba、TF-IDF 本身无随机性。
2. **jieba HMM 新词发现为默认开启**——不同 jieba 版本切分可能微异，复现请锁定 0.42.1。
3. **两脚本子串兜底阈值不一致**（L1≥3字／L2≥2字），跨脚本计数不可直接比较。
4. **A方 TF-IDF 聚合为全词表均值（含零值）**，与 B方"出现词项均值"口径不同——定稿图F采用B方值（1.133/0.766/0.266），A方值仅存档，两口径**不可直接对表**（详见指令二十九、三十评估记录）。
5. **不可脚本复现项**（须人工环节）：B方全部脚本未归档；语境情感词典（emotion_valence_map）为人工逐场景核定产物；BERTopic 为外部平台（BFSU BERTopic）运行，本地无执行环境；句级核验数据 sent_level_check.json 的生成脚本未单独归档。
6. **文件名勘误（已执行）**：定稿版数据附录 `SnowNLP情感训练语料_neg_18575条_全表.txt` 与实际包内 18,576 条差 1（缺 1 条全角空白记录），**已于2026-09-07统一更正**：三处归档（定稿版数据附录、分析输出定稿包、笔者绘制图表定稿包）均已替换为与包内 neg.txt 字节一致的 18,576 条完整版，文件名与全部 README 引用同步更正。
7. **语料一致性**：BERTopic 输入与 `分析输出/corpus` 经文档声明 MD5 逐字一致；A/B 两方语料切分不同（162段/390句 vs 160段/483句）为既定口径差异。

---

## 十二、最小复现路径（A方全流程）

```bash
# 1. 环境：Python 3.13.14 venv，安装
pip install jieba==0.42.1 snownlp==0.12.3 gensim==4.4.0 scikit-learn==1.9.0 \
            networkx==3.6.1 numpy==2.5.2 matplotlib==3.11.1 pandas==3.0.5 openpyxl==3.1.5
# 2. 依序运行（工作区根目录）：
python 分析输出/scripts/macro_l1_density.py      # 图A数据（词典定稿+词元密度）
python 分析输出/scripts/macro_l2_positions.py    # 图C数据（位置策略）
python 分析输出/scripts/macro_l3_semantic.py     # 图B数据（M1语义距离+基准，seed=42）
python 分析输出/scripts/micro_sentiment.py       # 图D形态曲线+场景统计（A方口径）
python 分析输出/scripts/micro_supplement.py      # 段落密度+人物网络（A方口径）
python 分析输出/scripts/make_figures_final.py    # 图A–G（B方校准值硬编码）
# 3. B方校准值与语境情感词典：数据附录附录四/五（人工核定，不可脚本复现）
# 4. BERTopic：BFSU BERTopic 平台，参数见§八（本地不可复现）
```

---

*本清单所有参数值均出自归档脚本原文或当前环境实测；凡标注"脚本未归档/平台侧/人工核定"者，为复现链路上的已知断点，建议在论文数据附录中如实声明。*
